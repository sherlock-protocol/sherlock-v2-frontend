import React from "react"
import styles from "./ProtocolBalance.module.scss"

export type ProtocolBalancePageProps = {}

export const ProtocolBalancePage: React.FC<ProtocolBalancePageProps> = () => {
  return <div className={styles.container}>PROTOCOL BALANCE PAGE</div>
}
