type Position {
  id: ID!
  usdcAmount: Int!
  sherAmount: Int!
  expiration: Int! # timestamp in milliseconds
  owner: String! # owner's address
}

type Query {
  positions(owner: String!): [Position]
}
